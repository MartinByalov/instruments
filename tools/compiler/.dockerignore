# Игнорираме всички папки, които не са нужни за C# проекта
**/.git
**/.github
**/node_modules
**/.vscode
# Don't exclude Backend/ since that's what we need
# Игнорираме всички компилирани и временни файлове
**/bin
**/obj
**/out
# Optionally exclude Frontend if not needed
Frontend/
documents/